<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
    <channel>
        <title>Aayush's Blog</title>
        <link>{{ url_for('blog', _external=True) }}</link>
        <description>a place to experiment and express</description>
        <lastBuildDate>{{ last_build_date }}</lastBuildDate>
        <language>en-us</language>

        {% for blog in blogs %}
        <item>
            <title>{{ blog['title'] }}</title>
            <link>{{ url_for('go_to_blog', post_name=blog['permalink'], _external=True) }}</link>
            <description>{{ blog['description'] }}</description>
            <pubDate>{{ blog['datetime'] }}</pubDate>
            <guid>{{ url_for('go_to_blog', post_name=blog['permalink'], _external=True) }}</guid>
        </item>
        {% endfor %}
    </channel>
</rss>